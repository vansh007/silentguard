# Runtime dependencies for the API image only (torch is installed separately, CPU-only).
# Training/plotting deps (xgboost, matplotlib, pytest) are deliberately absent.
numpy>=1.24
scipy>=1.10
pandas>=2.0
wfdb>=4.1
scikit-learn>=1.3
joblib>=1.3
shap>=0.44
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
